{
  "name": "@khoralabs/vellum",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*",
    "vendor/libs/packages/*/**",
    "vendor/obp/packages/*/**",
    "vendor/relay/*/**"
  ],
  "scripts": {
    "format": "biome check --write .",
    "format:check": "biome check .",
    "submodules:init": "git submodule update --init --recursive",
    "submodules:update": "git submodule update --remote --merge vendor/obp vendor/relay vendor/libs && bun install",
    "submodules:update:obp": "git submodule update --remote --merge vendor/obp && bun install",
    "submodules:update:relay": "git submodule update --remote --merge vendor/relay && bun install",
    "submodules:update:libs": "git submodule update --remote --merge vendor/libs && bun install",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.10",
    "@types/bun": "latest",
    "husky": "^9.1.7",
    "typescript": "^5"
  }
}
