AI_GATEWAY_API_KEY=
AGENT_REVIEW_MODEL=
SKIP_AGENT_REVIEW=

# Absolute path to the daemon executable (or Bun entry). Prefer this in monorepo
# checkouts so clients never embed a relative apps/daemon URL.
# Example: VELLUM_DAEMON_BIN=/absolute/path/to/vellum/apps/daemon/src/index.ts
# VELLUM_DAEMON_BIN=
